[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"
[tool.pixi.workspace]
name = "diffmap"
channels = ["conda-forge"]
platforms = ["osx-arm64"]

[tool.pixi.pypi-dependencies]
diffmap = { path = ".", editable = true }

[tool.pixi.tasks]
